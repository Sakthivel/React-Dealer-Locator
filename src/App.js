import React, { PureComponent } from 'react';
import './App.css';

class App extends PureComponent {
  render() {
    return (
      <div className="App">
        <header className="App-header">
          <h1 className="App-title">Welcome to Dealer Locator</h1>
        </header>
        <p className="App-intro">We are trying build dealer locator with reactJS.</p>
      </div>
    );
  }
}

export default App;
